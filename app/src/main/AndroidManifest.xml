<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 이미지 업로드를 위해 갤러리, 카메라 권한 설정 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:name=".api.auth.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenKim"
        tools:targetApi="31">

        <!-- 여기서부터는 중복된 activity 태그를 삭제하세요. -->

        <activity
            android:name=".CalendarDayItem"
            android:exported="true" />
        <!-- 중복된 activity 태그 삭제 -->
        <activity
            android:name=".MainActivity2"
            android:exported="true" />
        <activity
            android:name=".PopupActivity"
            android:exported="true" />
        <activity
            android:name=".WithdrawActivity"
            android:exported="true" />
        <activity
            android:name=".MyPageActivity2"
            android:exported="true" />
        <activity
            android:name=".MyPageActivity1"
            android:exported="true" />
        <activity
            android:name=".CalendarActivity"
            android:exported="true" />

        <activity
            android:name=".DetailPostActivity"
            android:exported="true" />
        <activity
            android:name=".CommunityActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GreenKim">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity
            android:name=".PostActivity"
            android:exported="true" />
        <activity
            android:name=".CheckPostActivity"
            android:exported="true" />
        <activity
            android:name=".setgetActivity"
            android:exported="true" />

    </application>

</manifest>